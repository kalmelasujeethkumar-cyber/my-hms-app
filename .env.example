# MediCare HMS Backend Environment Configuration

PORT=5000
NODE_ENV=development

# Database Connection (PostgreSQL or MySQL or SQLite for local fallback)
DATABASE_URL="file:./dev.db"

# JWT Authentication Secret
JWT_SECRET="medicare_hms_super_secret_jwt_key_2026"

# Frontend Allowed Origin for CORS
CORS_ORIGIN="http://localhost:3000"
